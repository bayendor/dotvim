Abandoned pathogen, replaced with vundle on 6/2/2015.

Installing from scratch:
```
cd ~
git clone http://github.com/bayendor/dotvim.git ~/.vim
ln -s ~/.vim/vimrc ~/.vimrc
ln -s ~/.vim/gvimrc ~/.gvimrc

Launch vim, run :PluginInstall
